import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata('公告')

export default function AnnouncementsLayout({ children }: { children: React.ReactNode }) {
  return children
}