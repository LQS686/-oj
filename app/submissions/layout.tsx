import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata('提交记录')

export default function SubmissionsLayout({ children }: { children: React.ReactNode }) {
  return children
}