import { pageMetadata } from '@/lib/metadata'

export const metadata = pageMetadata('找回密码')

export default function ForgotPasswordLayout({ children }: { children: React.ReactNode }) {
  return children
}