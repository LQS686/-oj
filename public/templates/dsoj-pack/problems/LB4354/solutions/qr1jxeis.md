# B4354 [GESP202506 一级] 假期阅读

---

我们可以先计算一下，在理想情况下，小 A 的阅读能力有多强。他有 $t$ 天的时间，每天能读 $k$ 页，那么在整个假期中，他总共能够阅读的页数就是 $k$ 乘以 $t$。代表了小 A 在假期中的总阅读量上限。

但是，这里有一个重要的限制条件：书本的总页数只有 $n$ 页。即使小 A 的阅读能力再强，他也不可能读到书中不存在的页面。所以，他实际能读的页数，绝对不会超过书的总页数 $n$。

因此，我们只需要计算出 $k \times t$ 的值，然后将它与 $n$ 进行比较，取其中较小的一个作为最终答案即可。

```cpp
int ________; // 计算假期中的总阅读量上限
if (________) ________; // 和书本的总页数取较小值
else ________;
________ // 输出答案
```
