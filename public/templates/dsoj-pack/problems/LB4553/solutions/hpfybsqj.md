# B4553 [GESP202606 二级] 完全平方数计数

先读入两个整数 $l$ 和 $r$，它们表示要检查的范围。题目要求统计从 $l$ 到 $r$ 之间有多少个完全平方数，包含 $l$ 和 $r$ 本身。

一个数是完全平方数，意思是它可以写成 $i \times i$ 的形式，其中 $i$ 是正整数。所以我们可以从 $1$ 开始试，把 $i \times i$ 算出来，看它是不是落在 $l$ 到 $r$ 之间。

因为 $r$ 最大只有 $2000$，所以不用担心循环太多。只要 $i \times i$ 还没有超过 $r$，就继续检查。如果 $i \times i$ 大于等于 $l$，说明它在范围内，就把计数器加 $1$。

最后输出计数器 $c$。如果一个完全平方数都没有找到，$c$ 会一直是 $0$，直接输出 $0$ 就可以。

参考代码：

```cpp
int c = 0;
for (________;________;________) {
    int x = ________; // 把 i*i 算出来
    if (________) // 检查是否落在 l 和 r 之间
        ________; // 计数器增加
}
cout << c;
```
